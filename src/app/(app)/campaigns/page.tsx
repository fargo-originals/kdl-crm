import { CampaignList } from '@/components/app/campaigns/campaign-list';

export const metadata = { title: 'Campañas — KDL CRM' };

export default function CampaignsPage() {
  return <CampaignList />;
}
